[project]
name = "argus"
version = "2.0.0"
description = "RCASR on ARGus: risk-controlled applicability set retrieval for auditable visual regulatory compliance."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "openai>=1.0.0",
    "pillow>=11.0.0",
    "python-dotenv>=1.2.2",
    "jsonschema>=4.0.0",
    "numpy>=1.26.0",
]

# Model serving — only needed to run the VLM/judge pipeline against an
# OpenAI-compatible endpoint (vLLM / SGLang). Not needed to read the code or to
# author/validate rules. Install with: uv sync --group serving
[dependency-groups]
serving = [
    "torch>=2.5.0",
    "torchvision>=0.20.0",
    "transformers>=4.51.0",
    "accelerate>=1.0.0",
    "huggingface-hub>=0.28.0",
    "compressed-tensors>=0.15.0",
    "vllm>=0.20.0",
]

# RCASR analysis extras — only for re-running the experiment scripts (public parquet reading and
# the diagnostic figures). Install with: uv sync --group analysis
analysis = [
    "pyarrow>=17.0.0",
    "matplotlib>=3.9.0",
]

# This repository is run as scripts over a src/ tree, not installed as a package,
# so uv manages the environment without building the project itself.
[tool.uv]
package = false
